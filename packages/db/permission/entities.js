// import all the models
import models from '../models'

export const async generateEntities = async () => {
    const entities = {}
    for (let model of Object.keys(models)) {
        const entity = 
        entities[model.modelName] = 
    }
    return entities
    }